/**
 * @deprecated DEAD CODE compatibility shim.
 *
 * The live orchestrator moved to ../dnaWorkflowOrchestrator.js. This file is
 * retained only so legacy imports fail loudly in review while still resolving
 * safely at runtime until all dead paths are removed.
 */

export { default } from "../dnaWorkflowOrchestrator.js";
export * from "../dnaWorkflowOrchestrator.js";
